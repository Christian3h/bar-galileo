# Configuración para Django Bar Galileo

# Clave secreta de Django (generada automáticamente para desarrollo)
secret_key=django-insecure-bar-galileo-dev-key-2024-!@#$%^&*()_+-={}[]|;:,.<>?

# Credenciales para envío de correos (Gmail SMTP)
emailHost=tu_email@gmail.com
emailPassword=tu_password_de_aplicacion

# Configuración de base de datos (SQLite por defecto)
# DATABASE_URL=sqlite:///db.sqlite3

# Configuración de Google OAuth (opcional)
# GOOGLE_CLIENT_ID=tu_google_client_id
# GOOGLE_CLIENT_SECRET=tu_google_client_secret

# Configuración de producción (cambiar en producción)
# DEBUG=False
# ALLOWED_HOSTS=tu_dominio.com,otro_dominio.com
