# --- DEPENDENCIAS ESENCIALES PARA BAR GALILEO ---

# Django y extensiones principales
Django==5.2.4
channels==4.3.1
django-allauth==65.10.0
django-dbbackup==5.0.0
django-markdown-deux==1.0.6
django-ranged-response==0.2.0
django-simple-captcha==0.6.2

# Utilidades y librerías básicas
python-dotenv==1.1.1
requests==2.32.4
pillow==11.3.0
PyYAML==6.0.2
sqlparse==0.5.3

# Seguridad y autenticación
cryptography==45.0.5
pyOpenSSL==25.1.0
PyJWT==2.10.1

# Base de datos MySQL/MariaDB
mysqlclient==2.2.7

# Paquetes para exportación de datos
openpyxl==3.1.5
reportlab==4.2.5

# --- DEPENDENCIAS ESENCIALES ADICIONALES ---
numpy==1.26.4
pymupdf==1.24.13
pytesseract==0.3.13

# --- IA / Embeddings / Búsqueda vectorial (alternativas livianas) ---
gensim==4.3.2           # Word2Vec, FastText, Doc2Vec embeddings
scikit-learn==1.4.2     # TF-IDF, NearestNeighbors, clustering, etc.
nltk==3.8.1             # Procesamiento de lenguaje natural clásico
spacy==3.7.4            # Procesamiento de texto eficiente y liviano

# --- DEPENDENCIAS OPCIONALES (para características avanzadas) ---
# Instala solo si necesitas RAG, OCR, PDF, IA, etc.

# PDF y OCR
# pymupdf==1.24.13
# pytesseract==0.3.13

# Backups automáticos con GPG (solo si usas backup cifrado)
# python-gnupg==0.5.5

# Servidor ASGI para producción
gunicorn==23.0.0
uvicorn==0.23.2
websockets
wsproto

# --- FIN DE DEPENDENCIAS ---

# NOTA:
# - Elimina dependencias duplicadas o pesadas que no uses.
# - Si necesitas alguna dependencia opcional, descomenta la línea correspondiente.
# - Para desarrollo avanzado, instala dependencias opcionales solo cuando lo requieras.
